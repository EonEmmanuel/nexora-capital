export type PaymentIntent={reference:string;provider:string;walletAddress:string;currency:string;network:string;amount:string;expiresAt:Date};
export interface PaymentProvider{createPaymentIntent(input:{amount:string;currency:string;network:string;allocationReference:string}):Promise<PaymentIntent>;getPaymentStatus(reference:string):Promise<'PENDING'|'DETECTED'|'COMPLETED'|'FAILED'>;verifyWebhook(payload:unknown,signature:string|null):Promise<{eventId:string;reference:string;status:string}>}
export class MockPaymentProvider implements PaymentProvider{async createPaymentIntent(input:{amount:string;currency:string;network:string;allocationReference:string}){return {reference:`MOCK-${input.allocationReference}`,provider:'mock',walletAddress:'TMockNexoraDemoWalletAddressDoNotSendFunds',currency:input.currency,network:input.network,amount:input.amount,expiresAt:new Date(Date.now()+30*60*1000)}}async getPaymentStatus(){return 'PENDING' as const}async verifyWebhook(){return {eventId:`mock-${Date.now()}`,reference:'mock',status:'COMPLETED'}}}
export const paymentProvider=new MockPaymentProvider();
