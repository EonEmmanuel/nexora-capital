import { notFound } from 'next/navigation';import { requireUser } from '@/server/auth/session';import { prisma } from '@/server/db/prisma';import { money } from '@/components/shared/format';
export default async function AllocationDetail({params}:{params:Promise<{allocationId:string}>}){const user=await requireUser();const {allocationId}=await params;const a=await prisma.allocation.findFirst({where:{id:allocationId,userId:user.id},include:{pool:true,transactions:true,withdrawals:true}});if(!a)notFound();return <section><span className="badge">{a.status}</span><h1>{a.pool.name}</h1><div className="grid" style={{gridTemplateColumns:'repeat(4,1fr)'}}>{[['Initial',a.initialValue],['Current',a.currentValue],['Realized P&L',a.realizedProfit],['Unrealized P&L',a.unrealizedProfit]].map(([label,value])=><div className="card" style={{padding:18}} key={String(label)}><p className="muted">{String(label)}</p><h2>{money(String(value),a.currency)}</h2></div>)}</div><div className="grid" style={{gridTemplateColumns:'1fr 1fr',marginTop:24}}><div className="card" style={{padding:24}}><h2>Performance</h2><svg viewBox="0 0 600 180" width="100%"><polyline points="20,140 120,130 220,100 320,108 420,70 560,52" fill="none" stroke="#54d6ff" strokeWidth="4"/></svg></div><div className="card" style={{padding:24}}><h2>Important dates</h2><p>Created: {a.createdAt.toLocaleDateString()}</p><p>Activated: {a.activatedAt?.toLocaleDateString() ?? 'Pending'}</p><p className="muted">Withdrawal eligibility depends on active status, profits/distributions, KYC, and platform rules.</p></div></div><h2>Transaction activity</h2><table className="table"><tbody>{a.transactions.map(t=><tr key={t.id}><td>{t.reference}</td><td>{t.type}</td><td>{money(t.amount.toString(),t.currency)}</td><td>{t.status}</td></tr>)}</tbody></table></section>}
