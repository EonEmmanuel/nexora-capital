import Link from 'next/link';import { requireUser } from '@/server/auth/session';import { prisma } from '@/server/db/prisma';import { createTicketAction } from '@/app/actions/support';
export default async function Support(){const user=await requireUser();const tickets=await prisma.supportTicket.findMany({where:{userId:user.id},orderBy:{createdAt:'desc'}});return <section><h1>Support</h1><form action={createTicketAction} className="card grid" style={{padding:24}}><h2>Create ticket</h2><input name="subject" placeholder="Subject"/><select name="category"><option>Account</option><option>Payment</option><option>Withdrawal</option><option>Pool</option><option>Technical</option></select><select name="priority"><option>Normal</option><option>High</option><option>Urgent</option></select><textarea name="message" placeholder="How can we help?"/><button className="btn btn-primary">Create ticket</button></form><h2>Your tickets</h2>{tickets.map(t=><Link className="card" style={{display:'block',padding:18,color:'white',textDecoration:'none',marginBottom:12}} href={`/dashboard/support/${t.id}`} key={t.id}>{t.subject}<p className="muted">{t.category} • {t.status}</p></Link>)}</section>}
