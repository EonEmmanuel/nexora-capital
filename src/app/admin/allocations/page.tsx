import { requireAuthorized } from '@/server/auth/session';import { prisma } from '@/server/db/prisma';import { money } from '@/components/shared/format';
export default async function Allocations(){await requireAuthorized('allocations.read');const rows=await prisma.allocation.findMany({include:{user:true,pool:true,payments:true,transactions:true},orderBy:{createdAt:'desc'},take:100});return <section><h1>Allocation management</h1><table className="table"><thead><tr><th>Reference</th><th>User</th><th>Pool</th><th>Initial</th><th>Current</th><th>Status</th><th>Created</th><th>Activated</th></tr></thead><tbody>{rows.map(a=><tr key={a.id}><td>{a.reference}</td><td>{a.user.email}</td><td>{a.pool.name}</td><td>{money(a.initialValue.toString(),a.currency)}</td><td>{money(a.currentValue.toString(),a.currency)}</td><td>{a.status}</td><td>{a.createdAt.toLocaleDateString()}</td><td>{a.activatedAt?.toLocaleDateString()??'—'}</td></tr>)}</tbody></table></section>}
