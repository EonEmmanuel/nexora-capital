import { requireAuthorized } from '@/server/auth/session';import { prisma } from '@/server/db/prisma';
export default async function AuditLogs(){await requireAuthorized('audit.read');const logs=await prisma.auditLog.findMany({include:{actor:true},orderBy:{createdAt:'desc'},take:200});return <section><h1>Audit logs</h1><table className="table"><thead><tr><th>Timestamp</th><th>Actor</th><th>Action</th><th>Entity</th><th>Description</th></tr></thead><tbody>{logs.map(l=><tr key={l.id}><td>{l.createdAt.toLocaleString()}</td><td>{l.actor?.email??'System'}</td><td>{l.action}</td><td>{l.entityType} {l.entityId}</td><td>{l.description}</td></tr>)}</tbody></table></section>}
