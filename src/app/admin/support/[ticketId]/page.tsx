import { notFound } from 'next/navigation';import { requireAuthorized } from '@/server/auth/session';import { prisma } from '@/server/db/prisma';import { supportAdminReplyAction } from '@/app/actions/admin';
export default async function Ticket({params}:{params:Promise<{ticketId:string}>}){await requireAuthorized('support.manage');const {ticketId}=await params;const t=await prisma.supportTicket.findUnique({where:{id:ticketId},include:{user:true,messages:true}});if(!t)notFound();return <section><h1>{t.subject}</h1><p className="muted">{t.user.email} • {t.category} • {t.priority} • {t.status}</p><div className="card" style={{padding:18}}>{t.messages.map(m=><article key={m.id}><p>{m.body}</p><p className="muted">{m.createdAt.toLocaleString()}</p></article>)}</div><form action={supportAdminReplyAction.bind(null,t.id)} className="card grid" style={{padding:18,marginTop:18}}><textarea name="message" placeholder="Reply to user"/><select name="status"><option>OPEN</option><option>PENDING</option><option>RESOLVED</option><option>CLOSED</option></select><button className="btn btn-primary">Reply and update</button></form></section>}
