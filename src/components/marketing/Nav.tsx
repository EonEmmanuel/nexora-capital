import Link from 'next/link';import { brand } from '@/config/brand';
export function Nav(){const links=[['/pools','Trading Pools'],['/how-it-works','How It Works'],['/security','Security'],['/about','Company']];return <header style={{position:'sticky',top:0,zIndex:10,backdropFilter:'blur(16px)',borderBottom:'1px solid #1d3045'}}><nav className="container" style={{height:72,display:'flex',alignItems:'center',justifyContent:'space-between'}}><Link href="/" style={{fontWeight:900,color:'white',textDecoration:'none'}}><span className="badge">{brand.logo}</span> {brand.companyName}</Link><div style={{display:'flex',gap:22,alignItems:'center'}}>{links.map(([h,l])=><Link key={h} href={h} className="muted" style={{textDecoration:'none'}}>{l}</Link>)}<Link href="/auth/sign-in" className="btn btn-ghost">Sign In</Link><Link href="/pools" className="btn btn-primary">Explore Pools</Link></div></nav></header>}
