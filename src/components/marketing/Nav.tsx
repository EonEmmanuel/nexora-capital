import Link from 'next/link';import { brand } from '@/config/brand';
export function Nav(){const links=[['/pools','Trading Pools'],['/how-it-works','How It Works'],['/security','Security'],['/about','Company']];const renderLinks=()=> <>{links.map(([h,l])=><Link key={h} href={h} className="muted" style={{textDecoration:'none'}}>{l}</Link>)}<Link href="/auth/sign-in" className="btn btn-ghost">Sign In</Link><Link href="/pools" className="btn btn-primary">Explore Pools</Link></>;return <header style={{position:'sticky',top:0,zIndex:10,backdropFilter:'blur(16px)',borderBottom:'1px solid #1d3045'}}><nav className="container nav-bar"><Link href="/" style={{fontWeight:900,color:'white',textDecoration:'none'}}><span className="badge">{brand.logo}</span> {brand.companyName}</Link><div className="nav-desktop">{renderLinks()}</div><details className="nav-mobile"><summary className="btn btn-ghost" aria-label="Open navigation menu">Menu</summary><div className="mobile-menu" role="navigation" aria-label="Mobile navigation">{renderLinks()}</div></details></nav></header>}
