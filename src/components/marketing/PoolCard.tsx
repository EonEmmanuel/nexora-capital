import Link from 'next/link';import { Pool } from '@/features/pools/data';import { money,pct } from '@/components/shared/format';
export function PoolCard({pool}:{pool:Pool}){const used=pool.allocated/pool.capacity*100;return <article className="card" style={{padding:24}}><div style={{display:'flex',justifyContent:'space-between'}}><span className="badge">{pool.risk}</span><span className="muted">{pool.status}</span></div><h3>{pool.name}</h3><p className="muted">{pool.strategy}</p><div className="grid" style={{gridTemplateColumns:'1fr 1fr',margin:'22px 0'}}><b>{money(pool.min)}</b><b>{pct(pool.historical)}</b><span className="muted">Minimum</span><span className="muted">Historical, not guaranteed</span></div><div style={{height:8,background:'#142236',borderRadius:99,overflow:'hidden'}}><div style={{width:`${used}%`,height:'100%',background:'linear-gradient(90deg,#2ff2a0,#54d6ff)'}} /></div><p className="muted">{used.toFixed(0)}% capacity utilized • {pool.duration}</p><Link className="btn btn-primary" href={`/pools/${pool.slug}`}>View pool</Link></article>}
